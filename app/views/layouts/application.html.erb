<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta lang="en">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "PUE Pomoc" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "PUE Pomoc" %>">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <%= favicon_link_tag asset_path('favicon.ico'), :rel => 'shortcut icon', :type => "image/x-icon" %>
  </head>

  <body class="site">
    <%= render 'layouts/header' %>
    <main class="site-content">
      <%= yield %>
    </main>
    <%= render 'layouts/footer' %>
  </body>
</html>
